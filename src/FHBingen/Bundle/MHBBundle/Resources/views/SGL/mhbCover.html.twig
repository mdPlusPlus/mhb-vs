<!DOCTYPE html>
<html>
<head>
    <title>Deckblatt des Modulhandbuchs für den Studiengang {{ studiengang }}</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <style type="text/css">
        html, body, #coverDiv {
            height: 100%;
        }

        #coverDiv {
            text-align: center;
        }

        #topDiv, #middleDiv, #bottomDiv {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        #topDiv, #bottomDiv {
            height: 20%;
        }

        #middleDiv {
            height: 60%;
        }

        span#fachbereich {
            font-weight: bold;
        }
    </style>
</head>
<body>
<div id="coverDiv">
    <div id="topDiv">
        <h1>
            Modulhandbuch<br/>
            {{ studiengang }} ({{ studiengang.kuerzel }})
        </h1>
    </div>

    <div id="middleDiv">
        {# funktioniert nur auf VM, nicht lokal #}
        <img src="{{ asset('bundles/fhbingenmhb/images/fh.jpg', absolute=true) }}" alt="FH Logo" width="80%"/>
    </div>

    <div id="bottomDiv">
        <p>
        <span id="fachbereich">
            {% if studiengang.fachbereich == 1 %}
                Fachbereich 1 - Life Sciences and Engineering
            {% endif %}
            {% if studiengang.fachbereich == 2 %}
                Fachbereich 2 - Technik, Informatik und Wirtschaft
            {% endif %}
        </span>
            <br/>

            {{ mhb.beschreibung }} <br/>

            <br/>
            <br/>

            Studiengangleiter: {{ studiengang.sgl }}<br/>
            Erstellt am {{ datum }}<br/>
            Gültig ab {{ mhb.gueltigAb }}
        </p>
    </div>
</div>
</body>
</html>