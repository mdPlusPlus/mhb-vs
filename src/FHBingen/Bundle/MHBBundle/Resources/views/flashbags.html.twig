{% for type, messages in app.session.flashBag.all %}
    {% for message in messages %}
        {% if type == 'info' %}
            <script type="text/javascript">
                alert('{{ message|raw }}');
            </script>
        {% endif %}
    {% endfor %}
{% endfor %}