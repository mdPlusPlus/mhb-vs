{% if app.user %}
    {% if is_granted('ROLE_SGL') %}
        <li role="presentation" class="active">
            <a>Administration</a>
        </li>
        <li role="presentation">
            <a id="benutzerverwaltungLink" href="{{ path('benutzerVerwaltung') }}">Benutzerverwaltung</a>
        </li>
        <li role="presentation">
            <a id="studiengangverwaltungLink" href="{{ path('studiengangVerwaltung') }}">Studiengangverwaltung</a>
        </li>
    {% endif %}
{% endif %}
