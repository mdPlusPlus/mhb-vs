{% if app.user %}
    {% if is_granted('ROLE_SGL') %}
        <li role="presentation" class="active">
            <a>Administration</a>
        </li>
        <li role="presentation">
            {# TODO route #}
            {# {{ path('benutzerverwaltung') }} #}
            <a href="{{ path('fhbingen_mhb_verwaltungs_sglshowusers') }}">Benutzerverwaltung</a>
        </li>
        <li role="presentation">
            {# TODO route #}
            {# {{ path('studiengangverwaltung') }} #}
            <a href="">Studiengangverwaltung</a>
        </li>
    {% endif %}
{% endif %}
